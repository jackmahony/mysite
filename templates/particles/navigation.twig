<nav style="background: url('{{ TimberImage(post.header_banner) }}') no-repeat top;" class="fadeInDown animated" id="topnav"><div class="row expanded columns"><div class="homenav"><ul class="menu">{% for item in topnav.get_items %}<li class="{{item.classes | join(' ')}}"><a href="{{item.link}}">{{item.title}}</a></li>{% endfor %}</ul></div></div><div class="row small-middle zoomIn animated"><img src="{{site.theme.link}}/assets/img/jm-logo.svg" />{% if post.tagline %}<h2>{{post.tagline}}</h2>{% endif %}</div></nav>